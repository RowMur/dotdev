---
import Tag from "./Tag.astro";

type Props = {
  tags: string[];
  activeTag?: string | undefined;
};

const { tags, activeTag }: Props = Astro.props;
---

<div class="flex gap-2 flex-wrap">
  {tags.map((tag) => <Tag text={tag} isActive={tag === activeTag} />)}
</div>
